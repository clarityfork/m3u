name: Combine M3U Playlists

on:
  schedule:
    - cron: '0 0 * * *'  # Runs daily at midnight UTC
  workflow_dispatch:     # You can trigger it manually too

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Combine M3U files
        run: |
          echo "#EXTM3U" > combined.m3u
          while read url; do
            echo "\n# Begin: $url" >> combined.m3u
            curl -s "$url" >> combined.m3u
          done < sources.txt

      - name: Commit and push changes
        run: |
          git config user.name "GitHub Actions Bot"
          git config user.email "actions@github.com"
          git add combined.m3u
          git commit -m "Update combined.m3u" || echo "No changes to commit"
          git push origin main
